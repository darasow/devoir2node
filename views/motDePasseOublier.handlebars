
  <div class=" dark:bg-gray-800 h-screen overflow-hidden flex items-center justify-center">
  <div class="bg-white lg:w-6/12 md:7/12 w-8/12 shadow-3xl rounded-xl">
    <div class="bg-gray-200 shadow shadow-gray-200 absolute left-1/2 transform -translate-x-1/2 -translate-y-1/2 rounded-full p-4 md:p-8">
       <img src="../images/logo.png" alt="" srcset="">
    </div>
    <form class="p-12 md:p-24 shadow shadow-gray-500" method="POST" action="/submit/motdepasse">

     <div class="flex items-center text-lg mb-6 md:mb-8">
    <svg xmlns="http://www.w3.org/2000/svg" class="absolute ml-3 h-5 w-5 text-gray-400 text-3xl" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.207" />
                    </svg>
    <input type="email" id="email" value="{{email}}" name="email" class="bg-gray-200 rounded pl-12 py-2 md:py-4 focus:outline-none w-full" placeholder="Votre email pour recuperer le mot de passe" />
</div>

      <button class="bg-gradient-to-b from-gray-700 to-gray-900 font-medium p-2 md:p-4 text-white uppercase w-full rounded">Continuer</button>
       {{#if message}}
        <div class="w-full font-bold my-2 text-xl flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-red-400 hover:bg-red-500 focus:outline-none focus:border-indigo-700 focus:shadow-outline-indigo transition duration-150 ease-in-out">{{message}}</div>
       {{/if}}
       {{#if passwordTrouver}}
        <div class="w-full font-bold my-2 text-xl flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-green-400  focus:outline-none focus:border-indigo-700 focus:shadow-outline-indigo transition duration-150 ease-in-out">{{passwordTrouver}}</div>
       {{/if}}
        <div class="flex flex-col md:flex-row justify-around items-center">
            <a href="/createAccount" class="  font-medium text-blue-500 rounded">Créer un compte</a>
            <a href="/" class="  font-medium text-blue-500 rounded">Se connecter</a>
        </div>
    </form>
  </div>
 </div>